import { serviceType } from './serviceType';
import { postType } from './postType';
import { jobType } from './jobType';
import { authorType } from './authorType';

export const schemaTypes = [authorType, serviceType, postType, jobType];
